import styled from "styled-components";
import BasicTitle from "./components/BasicTitle";
import { DefaultButton } from "./components/Buttons";

function App() {
  return (
    <div>
      <BasicTitle>Style Componenets</BasicTitle>
      <DefaultButton>click me</DefaultButton>
    </div>
  );
}

export default App;
